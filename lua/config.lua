-- `set` is a lot easier to remember
local set = vim.opt

-- Display tabs as being 4 spaces wide
set.tabstop = 4
set.shiftwidth = 4

-- Use hybrid line numbers
set.number = true
set.relativenumber = true

-- I like the mouse sometimes
set.mouse = 'a'
